name: Data Refresh

on:
  workflow_dispatch:
    inputs:
      mode:
        description: "Run mode"
        required: true
        default: "now"
        type: choice
        options:
          - now
          - daily
          - backfill
      date:
        description: "Target date (YYYY-MM-DD). Used mainly with mode=now or mode=daily."
        required: false
        type: string
      from:
        description: "Backfill start date (YYYY-MM-DD). Used with mode=backfill."
        required: false
        type: string
      to:
        description: "Backfill end date (YYYY-MM-DD). Used with mode=backfill."
        required: false
        type: string
      force:
        description: "Force refresh even if normalized file already exists."
        required: false
        default: false
        type: boolean
  schedule:
    # 09:10 / 12:10 / 15:10 / 18:10 JST: keep same-day partial snapshots
    - cron: "10 0,3,6,9 * * *"
    # 11:10 / 16:10 JST (02:10 / 07:10 UTC): refresh recent fixed datasets, retrying yesterday if publication was delayed
    - cron: "10 2,7 * * *"

permissions:
  contents: write

concurrency:
  group: data-refresh
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium

      - name: Run ingestion
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "schedule" ]; then
            if [ "${{ github.event.schedule }}" = "10 2,7 * * *" ]; then
              FROM="$(date -u -d '2 days ago' '+%Y-%m-%d')"
              TO="$(date -u -d '1 day ago' '+%Y-%m-%d')"
              echo "Scheduled fixed-data refresh for recent dates: ${FROM}..${TO}"
              npm run ingest -- --mode=backfill --from="${FROM}" --to="${TO}" --sample=daily --force=true
            else
              echo "Scheduled intraday refresh for current day"
              npm run ingest -- --mode=now --force=true
            fi
          else
            MODE="${{ github.event.inputs.mode }}"
            DATE="${{ github.event.inputs.date }}"
            FROM="${{ github.event.inputs.from }}"
            TO="${{ github.event.inputs.to }}"
            FORCE="${{ github.event.inputs.force }}"

            CMD=("npm" "run" "ingest" "--" "--mode=${MODE}" "--force=${FORCE}")
            if [ -n "${DATE}" ]; then
              CMD+=("--date=${DATE}")
            fi
            if [ -n "${FROM}" ]; then
              CMD+=("--from=${FROM}")
            fi
            if [ -n "${TO}" ]; then
              CMD+=("--to=${TO}")
            fi

            echo "Manual run command: ${CMD[*]}"
            "${CMD[@]}"
          fi

      - name: Commit normalized data if changed
        shell: bash
        run: |
          if [ -z "$(git status --porcelain -- data/normalized)" ]; then
            echo "No changes in data/normalized"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/normalized
          git commit -m "chore(data): refresh normalized dashboard data"
          git push
